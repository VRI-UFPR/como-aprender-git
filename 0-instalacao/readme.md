# 0. Instalação no Ubuntu ou Debian

```bash
sudo apt install git
```

# 1. Cadastro de uma nova chave SSH no Github

```bash
ssh-keygen
cat ~/.ssh/id_rsa.pub
```

![git-log](/imagens/instalacao-ssh.gif)

Voce pode colocar uma senha em sua chave, o que seria mais seguro.